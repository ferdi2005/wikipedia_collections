{% set active = orderBy|default == field %}
<a href="{{ path(route, {orderBy: field, ascending: active and not ascending}|merge(routeParams|default({}))) }}"
    class="order {{ active ? 'active' }} {{ active and ascending ? 'ascending' }}">
    {{ label|default|raw ?: field|capitalize }}
</a>